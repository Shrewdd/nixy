{ config, pkgs, ... }:

{
  # Display settings for this host have been moved to
  # `../../shared/nixos/display/common.nix` and
  # `../../shared/nixos/display/gnome.nix`.
  # Keep this file as a harmless placeholder during refactor.

}
