{ config, pkgs, ... }:

{
  # This host-specific display module has been largely moved to
  # `shared/nixos/display/*`. NVIDIA-specific settings were extracted
  # to `./nvidia.nix`. Keep this file as a harmless placeholder so
  # older imports won't fail during an intermediate refactor.

}